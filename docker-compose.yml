version: '3.8'
services:
  frontend:
    build: ./frontend
    ports:
      - "4200:4200"
    depends_on:
      - backend
  backend:
    build: ./backend
    ports:
      - "5010:5000"
    volumes:
      - ./backend:/app  
      - /app/node_modules  
    command: ["nodemon", "server.js"]
